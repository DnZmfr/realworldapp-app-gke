apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-configmap
data:
  mongo-init.js: |
    db.createUser({
      user: 'test',
      pwd: 'test',
      roles: [{role: 'readWrite', db: 'test'}]
    });
